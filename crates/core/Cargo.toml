[package]
name = "pyfastgrep-core"
version = "0.1.1"
edition = "2021"

[dependencies]
grep = "0.3"
grep-regex = "0.1"
grep-searcher = "0.1"
ignore = "0.4"
rayon = "1.8"
crossbeam-channel = "0.5"
globset = "0.4"
serde = { version = "1.0", features = ["derive"] }
